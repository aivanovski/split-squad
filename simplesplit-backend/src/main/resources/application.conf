h2db {
  dataSourceClassName = "org.h2.jdbcx.JdbcDataSource"
  dataSource {
    url = "jdbc:h2:mem:split_it_db;DB_CLOSE_DELAY=-1;INIT=RUNSCRIPT FROM 'classpath:init.sql'" # Using in-memory DB for now, with init script
    user = "sa"
    password = ""
  }
}